import React from "react";
import { Outlet } from "react-router-dom";

const TicketLayout = () => {
  return (
    <div>
      TicketLayout
      <Outlet />
    </div>
  );
};

export default TicketLayout;
